import type { AnalysisResult } from '../types';

export default function TrendingPage({ scans, onOpen }: { scans: AnalysisResult[]; onOpen: (id: string) => void }) {
  return <section className="glass-panel rounded-[2rem] p-5 sm:p-8"><p className="text-xs uppercase tracking-[0.35em] text-cyan-200/70">Public feed</p><h2 className="mt-2 text-3xl font-black text-white">Recently debunked</h2><p className="mt-2 text-sm text-slate-300">Public scans with the lowest credibility scores, sorted for review.</p><div className="mt-6 grid gap-3 md:grid-cols-2">{scans.length ? scans.map((scan) => <button key={scan.id} type="button" onClick={() => onOpen(scan.id)} className="rounded-2xl border border-white/10 bg-white/5 p-4 text-left transition hover:bg-white/10"><div className="flex items-start justify-between gap-3"><p className="font-semibold text-white">{scan.sourceTitle || scan.input}</p><span className="shrink-0 text-lg font-black text-rose-300">{scan.credibilityScore}</span></div><p className="mt-2 line-clamp-2 text-sm text-slate-400">{scan.summary}</p><p className="mt-3 text-xs text-slate-500">{new Date(scan.createdAt).toLocaleDateString()}</p></button>) : <p className="rounded-2xl border border-dashed border-white/10 p-6 text-sm text-slate-400">No public scans are available yet.</p>}</div></section>;
}
